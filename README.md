# Student Registration Form

## Start the project

Open the `sa.html` file in your browser.

The project contains a simple student registration form with:

- Name
- Email
- Gender
- Branch

Sample student data is displayed using a table.

The form is created using **HTML and CSS** with `div` and `span` elements.
